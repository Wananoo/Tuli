using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaneScript : MonoBehaviour
{
    [SerializeField] public GameObject SpawnX;
    [SerializeField] public GameObject SpawnXn;
    [SerializeField] public GameObject SpawnZ;
    [SerializeField] public GameObject SpawnZn;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
